const neumorphism = require('tailwindcss-neumorphism');

module.exports = {
  darkMode: 'media', // or 'media' or 'class'
  plugins: [neumorphism],
  purge: [],
  theme: { extend: {} },
  variants: { extend: {} },
};
