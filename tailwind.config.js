const neumorphism = require('tailwindcss-neumorphism');

module.exports = {
  darkMode: false, // or 'media' or 'class'
  plugins: [neumorphism],
  purge: [],
  theme: { extend: {} },
  variants: { extend: {} },
};
